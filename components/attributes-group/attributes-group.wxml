<view class="attributes-group">
    <block wx:for="{{attributes}}" wx:key="{{item}}">
        <view class="item">
            <text class="key">{{item.key}}</text>
            <text class="value">{{item.value}}</text>
        </view>
    </block>

</view>